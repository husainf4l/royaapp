import 'package:flutter/material.dart';

class SmartReplayScreen extends StatelessWidget {
  const SmartReplayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Smart Replay Screen'));
  }
}
